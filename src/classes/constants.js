const vehicleType = {
  CAR: 'car',
};

const NO_EMPTY_SPOTS = 'no empty spots';
const VEHICLE_NOT_FOUND = 'vehicle not found';

const PARKING_LOT_FULL = 'Sorry, parking lot is full';
const VEHICLE_ALREADY_PARKED = 'Sorry, car already parked in lot';

module.exports = {
  vehicleType,
  NO_EMPTY_SPOTS,
  VEHICLE_NOT_FOUND,
  PARKING_LOT_FULL,
  VEHICLE_ALREADY_PARKED,
};
